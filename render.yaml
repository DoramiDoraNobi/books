services:
  - type: web
    name: books-api
    env: node
    plan: free
    region: oregon
    buildCommand: "npm install"
    startCommand: "node src/app.js"
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
